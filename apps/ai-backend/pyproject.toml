[project]
name = "ai-backend"
version = "0.1.0"
description = ""
authors = [
    {name = "Moritz Beck (Birkenstab)",email = "git@birkenstab.de"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi[standard] (>=0.128.0,<0.129.0)"
]

[tool.poetry]
packages = [{include = "ai_backend", from = "src"}]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

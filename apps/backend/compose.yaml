services:
    server:
        build:
            context: .
        environment:
            NODE_ENV: production
            PORT: ${PORT:-3000}
        ports:
            - ${PORT:-3000}:${PORT:-3000}

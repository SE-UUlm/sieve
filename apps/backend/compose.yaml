services:
    server:
        build:
            context: .
        environment:
            NODE_ENV: production
            PORT: ${PORT:-5175}
        ports:
            - ${PORT:-5175}:${PORT:-5175}
